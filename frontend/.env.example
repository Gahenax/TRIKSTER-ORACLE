# Frontend Environment Variables

# For local development (uses Vite proxy to localhost:8000):
VITE_API_URL=/api

# For production build, leave VITE_API_URL unset.
# The built-in domain fallback will be used automatically:
#   Primary:  https://trickster-api.gahenaxaisolutions.com
#   Fallback: https://trickster-oracle-api.onrender.com
